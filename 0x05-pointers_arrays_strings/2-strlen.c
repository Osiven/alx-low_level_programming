/**
 * _strlen - calculate the lenght of a string
 * @*s: pointer
 *
 * Return: always 0 .
 */
#include <stdio.h>
#include <string.h>
#include "main.h"

int _strlen(char *s)
{
	size_t strlen(const char *s);
}
